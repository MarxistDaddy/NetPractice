MAC address:

every device has to have a network inerface card, which is a card that we use to connect to with it with a cable to get the internet

used to renslate signsla to binary and vice versa

every internface card comes with a mac address
mac address conssits of 6 octal thats in hexa decimal

===========

DNS:

translate domain names to ip addresses

DNS servers:
servers are computers with high computential poiwers


================


DHCP:

you cant connect a device to a network without setting its IP address!
2 ways to set up the IP address: static and dynamic!

DGCP is a service that exists in the router, that provides IP addresses to the devices that are connected to it!

1-      static IP:
static IP is about setting the IP address of every device manually!
you need to make sure that each device has its own ip address!
subnest mask and default getway
==> this useful to use if you are in a nat wtih a small number of ip addresses


2-      dynamic IP:
ifyou are in a network that has like 200 
    

the mental model to choose when to use static or dynamic IPs:
    "If it serves, give it a static. If it surfs, give it a dynamic."

when you look at a device, ask yourself the following:
1- does a device depend on finding this device at a fixed network? a printer for example
    if yes, use static IP

2- do I, as a human, need to type the IP addess of this device to reach it?
    if yes, I need to make static, I cant memorise and find the IP of this device if its lease dynamically

3- does this device just ask for things, like does just search for other IP addresses like google,
    without providing anything to others?
if Yes, dynamic 
in this case, the no other device depends on thsi device! it completely independantable!


==> the provider and consumer model

provider == static == other devices depends on it
cinsumer == dynamic == doesnt need other devices

examples:

web server == provider == static
a printer == provider == static
your cluster nodes  == provider == static
your laptop == consumer == dynamic
your phone == consumer == dynamic


==> DHCP is a service, this means that it needs to run a server in order to provide this service to who request it
==> DHCP could be running on linux server, microsoft server, or built-in in a router as a service!


================

ARP:

say taht we have 2 copputers connected with either with a switch in between them

PC A -------> switch <-----------PC B

pc A is the source, and wants to send data to the target PC B
sinc PC A only knows the IP address of PC B, and has no idea of its MAC address

it relies on ARP: address resulotion protocol!
arp == links the IP address with the mac address of the computers

MAC address is the unique address thats provided in the pysical network interface card of a computer

in LAN, if you want to communicate other devices, you need to know its mac address
and devices uses this protocol its get the target mac address of the computer


how does the arp wors

say that we have 4 computers connected through a switch

computer A, B, C, D
A knows the IP address of computer B, and wants to send data to it!!!!!



IP address:
locate a device location in a network | which network on the internet this device exists
locate the network of the device

MAC address:
locate the device inside the network LAN
locates the actual device


for ARP to find the mac address of a device inside a network:
1- looks at ARP cache to see if its there
2- if it doesnt exists in the ARP cache, it will send a broadcast message to all the device in the network, and see which has this ip address, and ask it for its corresponding mac address!!!!!
if a computer doesnt have the matching IP address, it will ignore the broadcast message
==================================


how does the firewall works?

firewall comes into shapes: software or hardware

software == host-based firewall
used to protect a single device, your computer and thats it
its built in in our computer by default


hardware == network-based firewaill






firewall tyes:
1- packets filtering firewall:
packets; when you are downloging a file on the internet, like 1 gp, you are downloading in the form of packets
that 10gp, is broakendown into packets to handle it


when the your computer starts receving these packets, the firewall looks into:
1- IP sender
2- IP receiver
3- port nunber


port number is a number that exists at the end of the IP address


pecket filterting firewall is the cheapest and most firewall, it exists in all routers
its own responsibility is filter packets based on these 3 criteria: ip sender, receiver and port number

=======================================================================

DMZ: demelitirized zone

this is a network secutiry













































